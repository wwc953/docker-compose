# /etc/nginx/nginx.conf - 完整版

user www-data;
worker_processes auto;
pid /run/nginx.pid;
error_log /var/log/nginx/error.log;

events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

# Stream模块：四层TCP转发
stream {
    # 日志格式
    log_format stream_log '$remote_addr [$time_local] $protocol '
                         '$status $bytes_sent $bytes_received '
                         '$session_time "$ssl_preread_server_name"';

    access_log /var/log/nginx/stream.log stream_log;

    # 上游：本机HTTP模块的HTTPS端点
    upstream http_backend {
        server 127.0.0.1:8080;
    }

    # 监听443端口，转发到HTTP模块
    server {
        listen 443 reuseport;
        listen [::]:443 reuseport;

        proxy_pass http_backend;
        proxy_connect_timeout 5s;
        proxy_timeout 300s;

        # 启用TCP keepalive
        proxy_socket_keepalive on;

        # SSL预读（用于获取SNI）
        ssl_preread on;

        # 设置代理协议（可选）
        # proxy_protocol on;
    }
}

# HTTP模块：七层HTTP代理
http {
    # 基础配置
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # 日志格式
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    # 性能优化
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    server_tokens off;

    # 缓冲区设置
    client_body_buffer_size 128k;
    client_max_body_size 10m;
    client_header_buffer_size 1k;
    large_client_header_buffers 4 8k;

    # 代理设置
    proxy_redirect off;
    proxy_buffering off;
    proxy_request_buffering off;

    # 百度上游服务器（带健康检查）
    upstream baidu_backend {
        server www.baidu.com:80 max_fails=3 fail_timeout=10s;

        keepalive 32;
        keepalive_timeout 60s;
        keepalive_requests 100;
    }

    # HTTP模块服务器：接收Stream转发的HTTPS流量
#     server {
#         # 监听4443端口，带SSL终止
#         listen 4443 ssl http2;
#         listen [::]:4443 ssl http2;
#         server_name _;
#
#         # SSL证书配置
#         ssl_certificate /etc/nginx/ssl/self-signed.crt;
#         ssl_certificate_key /etc/nginx/ssl/self-signed.key;
#
#         # SSL优化
#         ssl_protocols TLSv1.2 TLSv1.3;
#         ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
#         ssl_prefer_server_ciphers off;
#         ssl_session_cache shared:SSL:10m;
#         ssl_session_timeout 10m;
#         ssl_session_tickets off;
#
#         # 超时设置
#         client_header_timeout 10s;
#         client_body_timeout 10s;
#         send_timeout 10s;
#
#         # 处理所有请求
#         location / {
#             # 代理到百度HTTP
#             proxy_pass http://baidu_backend;
#
#             # 设置HTTP头部
#             proxy_set_header Host www.baidu.com;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#             proxy_set_header X-Forwarded-Host $host;
#             proxy_set_header X-Forwarded-Port $server_port;
#             proxy_set_header X-Original-URI $request_uri;
#
#             # 自定义头部（根据需要）
#             proxy_set_header Referer "";
#             proxy_set_header Origin "";
#
#             # 超时设置
#             proxy_connect_timeout 5s;
#             proxy_send_timeout 10s;
#             proxy_read_timeout 30s;
#
#             # 连接池优化
#             proxy_http_version 1.1;
#             proxy_set_header Connection "";
#
#             # 错误处理
#             proxy_intercept_errors on;
#             error_page 500 502 503 504 =200 /error.html;
#
#             # 禁用缓存
#             proxy_no_cache 1;
#             proxy_cache_bypass 1;
#
#             # 添加响应头用于调试
#             add_header X-Proxy-Server "nginx-stream-proxy";
#             add_header X-Upstream-Status $upstream_status;
#             add_header X-Upstream-Addr $upstream_addr;
#             add_header X-Upstream-Response-Time $upstream_response_time;
#         }
#
#         # 错误页面
#         location = /error.html {
#             internal;
#             root /usr/share/nginx/html;
#         }
#
#         # 健康检查端点
#         location /health {
#             access_log off;
#             return 200 "OK\n";
#             add_header Content-Type text/plain;
#             add_header Cache-Control "no-cache, no-store, must-revalidate";
#         }
#
#         # 禁止访问隐藏文件
#         location ~ /\. {
#             deny all;
#         }
#     }

    # 可选：直接HTTP代理（不通过stream）
    server {
        listen 8080;
        server_name localhost;

        location / {
            proxy_pass http://baidu_backend;
            proxy_set_header Host www.baidu.com;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            add_header X-Test-Mode "Direct-HTTP" always;
        }
    }
}